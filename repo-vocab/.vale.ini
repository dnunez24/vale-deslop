# Not a distributable package: a local-only complete-package shell that lets
# `vale sync` install this repo's own prose vocabulary the same way it
# installs Deslop and Microsoft. Never referenced by consumers.
StylesPath = styles
